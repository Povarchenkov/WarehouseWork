#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)	
	
	Если Параметры.Свойство("АдресХранилищаКартинки") Тогда
		АдресКартинка = Параметры.АдресХранилищаКартинки;			
	КонецЕсли;
	Если Параметры.Свойство("ЗаголовокКартинки") Тогда
		ЗаголовокКартинки = Параметры.ЗаголовокКартинки;
	Иначе 
		Элементы.ЗаголовокКартинки.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти